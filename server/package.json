{
  "name": "patient-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "NODE_ENV='development' nodemon --exec babel-node -- ./bin/www",
    "build": "NODE_ENV='production' babel ../server --out-dir lib",
    "serve": "node app-compiled.js"
  }
}
